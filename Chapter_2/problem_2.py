# Define conversion value
total_sales = 0.23
# We ask the user what the projected sales are
sales = float(input("What is the projected number of total sales?"))
profit = sales * total_sales
# Tell user their total profit
print("Your total profit is $" + str(float(profit)))